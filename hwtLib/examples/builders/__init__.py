"""
Example of usage of builder classes. Builder class is something which takes interface as an input and can be used to automatically instantiate, configure and connect
various component to that interface.
Components like buffers, CDC, parsers, inteconects, decoders, interface adapters can be easily instantiated using such a class.
"""